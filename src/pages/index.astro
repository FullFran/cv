---
import Layout from "@layouts/Layout.astro";
import About from "@components/sections/About.astro";
import Education from "@/components/sections/Education.astro";
import Experience from "@/components/sections/Experience.astro";
import Hero from "@/components/sections/Hero.astro";
import Projects from "@/components/sections/Projects.astro";
import Publications from "@/components/sections/Publications.astro";
import Skills from "@/components/sections/Skills.astro";
import KeyboardManager from "@/components/KeyboardManager.astro";
import { data, type Locale } from "@lib/cv";

const locales: Locale[] = ["en", "es"];
---

<Layout title="Francisco Olmedo — CV">
	<main
		id="main-content"
		tabindex="-1"
		data-tui-title="fullfran/cv"
		class="mx-auto my-4 w-[min(100%-1rem,72rem)] rounded-2xl bg-white px-4 py-6 pb-28 text-gray-800 shadow-sm ring-1 ring-gray-200 sm:my-6 sm:w-[min(100%-2rem,72rem)] sm:px-6 sm:py-8 md:px-8 md:py-10 md:pb-28 lg:w-[min(100%-3rem,76rem)] print:my-0 print:w-full print:rounded-none print:shadow-none print:ring-0"
	>
		{
			locales.map((lng) => (
				<div data-lang-wrap={lng}>
					<Hero data={data[lng]} lang={lng} />
					<About data={data[lng]} lang={lng} />
					<Experience data={data[lng]} lang={lng} />
					<Projects data={data[lng]} lang={lng} />
					<Publications data={data[lng]} lang={lng} />
					<Education data={data[lng]} lang={lng} />
					<Skills data={data[lng]} lang={lng} />
				</div>
			))
		}
	</main>

	<KeyboardManager />
</Layout>
